// X (Twitter) uses the same preview card as every other platform.
export { default, alt, size, contentType } from './opengraph-image';

// Route config can't be re-exported, so the runtime is declared here too.
export const runtime = 'edge';
